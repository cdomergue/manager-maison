// Environnement de production
export const environment = {
  production: true,
  // Cette URL sera injectée par AWS Amplify via les variables d'environnement
  apiUrl: 'API_URL_TO_BE_REPLACED', // Placeholder qui sera remplacé à la compilation
};
